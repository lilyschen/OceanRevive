import React, { useState } from 'react';
import './Profile.css';

const Profile = () => {

    return (
        <div className="profile-container">
            <img src={require('./UI icon/Scroll_group.png')} className="TS" />
        </div>
    );
};

export default Profile;

